#!/bin/bash

INSTDIR=${PAPI_DIR}

mkdir -p ${INSTDIR}
rm -rf ${INSTDIR}/*

make clobber
#./configure --prefix=${INSTDIR} --with-components="" --with-debug=yes
./configure --prefix=${INSTDIR} --with-components=""

make #-j8
make install
