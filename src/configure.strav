#!/bin/bash

export INSTDIR=${PAPI_DIR}

mkdir -p ${INSTDIR}
rm -rf ${INSTDIR}/*

make clobber

#./configure --prefix=${INSTDIR} --with-components=""
#./configure --prefix=${INSTDIR} --with-components="" --with-debug=yes
#./configure --prefix=${INSTDIR} --with-components="rapl powercap"
./configure --prefix=${INSTDIR} --with-components="cuda"
